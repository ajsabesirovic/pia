/**
 * Base URL of the existing Laravel backend's additive JSON API.
 * The PHP app is served by `php artisan serve` on port 8000.
 */
export const API_URL = 'http://localhost:8000/api';

/** Key under which the Sanctum bearer token is persisted. */
export const TOKEN_KEY = 'farmacy_token';
